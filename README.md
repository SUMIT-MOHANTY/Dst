# Project Showcase Grid

## Setup
1. Open `src/index.html` in a browser or serve via a local server.
2. Modify `src/js/main.js` to update project data.

## Security Features
- Content Security Policy (CSP) meta tag implemented.
- Input sanitization utility used before rendering HTML.
- Semantic HTML used for accessibility compliance.
