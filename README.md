# Web Performance Optimizer

Optimize images to WebP and minify CSS/JS files.

## Usage

1. Place images in `input/images/`
2. Place CSS files in `input/css/`
3. Place JS files in `input/js/`
4. Run: `python optimize.py`
5. Find optimized files in `output/`
