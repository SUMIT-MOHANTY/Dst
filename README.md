# Skills Visualizer Component

## Features
- Dynamic rendering of skills based on JSON data.
- Accessible design respecting `prefers-reduced-motion`.
- XSS protection via input sanitization.

## Usage
Open `index.html` in a browser.

## Security Notes
- Input is sanitized before DOM insertion.
- No external dependencies used to reduce supply chain risk.
