# Web Performance Optimizer

## Description
Optimizes images to WebP format and minifies CSS/JS files.

## Installation
pip install -r requirements.txt

## Usage
python optimizer.py

## Input Files
- Place images in /workspace/images/
- Place CSS files in /workspace/css/
- Place JS files in /workspace/js/

## Output
Optimized files will be in /workspace/dist/
